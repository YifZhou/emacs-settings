;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/Users/ZhouYf/.emacs.d/plugins/emacs-for-python/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 140)
        (left . 726)
        (width . 80)
        (height . 35)))

